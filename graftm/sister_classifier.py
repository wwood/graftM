import jplace

class SisterClassifier:
    def classify_jplace_file(self, jplace_fh, annotated_tree):
        pass
    
    def classify_edge(self, jplace_tree, edge_index):
        # given a jplace object and an edge index, annotate by assuming that
        # it is part of sister lineages.
        # return the taxonomy as an array
        